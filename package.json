{
	"name": "walnut-javascript",
	"version": "0.1.0",
	"scripts": {
		"dev-server": "http-server build -p 12080 -o index.html",
		"dev-copy": "copy-and-watch --watch example/** build",
		"dev": "npm-run-all --parallel dev-*"
	},
	"license": "MIT",
	"devDependencies": {
		"copy-and-watch": "^0.1.5",
		"npm-run-all": "^4.1.5"
	}
}
